apiVersion: v1
kind: Secret
metadata:
  name: hev-shop-secrets
type: Opaque
stringData:
  # Matches the bundled layer Helm chart defaults. Replace this for managed PG.
  LAYER_DATABASE_URL: postgres://hevlayer:hevlayer@layer-postgres.layer.svc.cluster.local:5432/hevlayer
  # Placeholder only. deploy-hev-shop.sh restores this from 1Password when available.
  OPENROUTER_API_KEY: ""
